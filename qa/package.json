{
  "name": "ai-agent-hub-qa",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "visual-qa": "npx playwright test visual-qa.spec.js --reporter=line"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@playwright/test": "^1.40.0"
  }
}
